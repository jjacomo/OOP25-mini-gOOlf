Report - 13/05 - 2h (tot: 40h)

* Sto guardando il codice di controller.game.GameFactory che e' dove viene creata la mappa.
    In teoria se si importa il controller della mappa non dovrebbe esserci bisogno di importare anche la mappa (chiedi a fede)
    Ma forse e' necessario per settare una nuova mappa (altrimenti un metodo setMap in GameMapController)
* Ho visto che ctx usa direttamente la palla per modificarne la velocita' invece del controller
    l'ho modificato (vecchio codice commentato) in MainControllerImpl
* Risolto il bug della palla che oscillava quando le costanti di attrito erano troppo grandi (deltaV troppo negativo).
* Cambiate le costanti di attrito ora sono molto piu' grandi, ora sarebbe da dire a fede di aumentare la potenza dell'input.
* Creato la costante per il vettore nullo in util.Vector2D, quindi rimpiazzato il codice dove si creava un vettore nullo
    con Vector2D.ZERO

TODO:
* Riguarda e aggiungi i commenti della javadoc;
* Guarda se avere getColor in tutte le shapes ha senso;

* Migliore algoritmo per l'update della velocita'

* Fare che se la palla va sull'acqua ritorna alla posizione precedente? (Con fede?)
* Dove creare GameMapController (per ora in GamePanel)
* Guarda errori piu' "Strutturali" di spotbugs
* Controllo errori (anche i piu' banali: ad esempio check che il raggio della pallina sia >0)
* Una factory per le superfici?
* Ma le altre classi del model dovrebbero usare direttamente la palla o il controller?
* Nelle shapes c'e' un getColor, dovrebbe essere implementato da ogni shape anche se e' uguale
    per tutte? (non sembra un granche') (Abstract?)

POSSIBILI FEATURE:
* Caricare mappe da file

* puoi far scegliere la strategy in base al terreno:
    velocityStrategy = strategyFactory.getStrategy(ball.getCurrentTerrain());
* oppure combinare più effetti (attrito + vento) (Decorator Pattern)

PER AGENDA FUTURA:
* MainController che li contiene tutti?
* Accumulatore per il dt nel game loop?

* Dovrebbe esserci una velocita' massima della palla? (Con fede)
* Guarda il primo punto delle cose fatte oggi